# Pinned exactly: the committed anchors in labels/ were generated against this
# build of graphify. A release that clusters differently would silently detach
# curated names from their communities.
graphifyy==0.9.6
